version: "3.1"
services:
  web:
    volumes:
      - ./:/srv
      - ../public:/srv/public
  polling:
    volumes:
      - ./:/srv
  mongo:
    ports:
      - 27017:27017
  redis:
    ports:
      - 6379:6379
